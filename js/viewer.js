const params=new URLSearchParams(location.search); const no=params.get('no'); const img=document.getElementById('imgVerse'); const loading=document.getElementById('loading'); if(!no){loading.textContent='找不到經句編號';} else{img.src='cards/'+no+'.png'; img.onload=()=>loading.textContent='';} document.getElementById('btnDownload').onclick=()=>{const a=document.createElement('a'); a.href=img.src; a.download='verse_'+no+'.png'; a.click();};